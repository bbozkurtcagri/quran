# Build outputs
**/bin/
**/obj/
**/out/
**/publish/

# IDE / editor
**/.vs/
**/.vscode/
**/.idea/
*.user
*.suo

# Git & CI
.git/
.gitignore
.gitattributes
.github/

# Tests — not needed for runtime image
tests/

# Docs / housekeeping
*.md
docs/
LICENSE

# Local state
docker/data/
**/appsettings.Local.json
**/appsettings.*.Local.json
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Seed data now lives in its own image (services/seed-data/Dockerfile) —
# init-container pattern: API pod mounts the data volume, so runtime API
# image stays lean. Data files must be in the build context for the
# seed-data image build to succeed.
